============
Installation
============

At the command line::

    $ easy_install django-oscar-shipping

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-oscar-shipping
    $ pip install django-oscar-shipping
